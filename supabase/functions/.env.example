# Edge Function secrets. Local: copy to supabase/functions/.env (gitignored,
# picked up by `supabase functions serve`). Production: `supabase secrets set`.

# Clerk issuer for in-function JWT verification (_shared/client.ts).
# Local dev instance:
CLERK_ISSUER=https://social-leech-62.clerk.accounts.dev
# Production: https://clerk.alternatif.space
