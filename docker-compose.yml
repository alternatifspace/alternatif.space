services:
  metabase:
    image: metabase/metabase:latest
    container_name: alternatif-metabase
    network_mode: host
    env_file: .env.metabase
    volumes:
      - metabase-data:/metabase-data
    restart: unless-stopped

volumes:
  metabase-data:
